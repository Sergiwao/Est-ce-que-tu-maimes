services:
  - type: web
    name: est-ce-que-tu-maimes
    runtime: node
    plan: free
    buildCommand: echo "no build"
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: "20"
